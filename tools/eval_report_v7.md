# 双源对比评估报告

## 1. 数据源
- **解析器**: 区域数 3，OCR 文案元素数 71
- **多模态**: 区域数 3，组件数 7

## 2. 区域对比

### 2.1 一致区域（名称对齐且文案重叠率 ≥ 50%）
- 主内容区
- 左侧边栏
- 顶部导航栏

### 2.2 仅解析器识别的区域
- 无

### 2.3 仅多模态识别的区域
- 无

### 2.4 文案重叠详情（解析器 vs 多模态）

- **主内容区**: 解析器 40 条，多模态 24 条，匹配 26 条，重叠率 0.65
  - 仅解析器有: ['Summary', 'Thisis', 'test test. Thisis a', 'test test. Thisis', 'testtest']
  - 仅多模态有: ['测试用例配置', '可选枚举值...', '可配置定制化中英文名称...']

- **左侧边栏**: 解析器 19 条，多模态 21 条，匹配 16 条，重叠率 0.84
  - 仅解析器有: ['DigitalCity 智薏域市', '项曰设置', '代码裣查']
  - 仅多模态有: ['项目设置', 'DigitalCity智慧城市', '项目设置', '代码检查', '安全']

- **顶部导航栏**: 解析器 12 条，多模态 9 条，匹配 7 条，重叠率 0.58
  - 仅解析器有: ['MINh', '萏页', '工怍台', '项目设置', '测试用例配置']
  - 仅多模态有: ['首页', '工作台']

## 3. 组件规格（多模态）

解析器(OCR)不输出组件类型，仅多模态提供；合并 Spec 时以多模态组件为主、用解析器文案做校验。

| 类型 | 区域 | 承载信息 |
|------|------|----------|
| Toggle | 主内容区 | 开启用例权限 |
| Toggle | 主内容区 | 允许手动标记结果 |
| Toggle | 主内容区 | 标记用例失败时必须输入原因 |
| Toggle | 主内容区 | 开启精准测试 |
| Table | 主内容区 | 字段名/字段类型/字段值，含输入框与删除 |
| Button | 主内容区 | 添加, 保存, 取消 |
| Table | 主内容区 | 字段中文/字段英文 |

## 4. 合并建议

- 区域划分：以多模态为主，用解析器各区域 `text_samples` 补全/校正漏识或错识文案。
- 组件规格：采用多模态的 `component_specification`，用解析器 OCR 结果校验按钮、输入框等承载文案是否一致。
- 冲突项：对「仅解析器有」「仅多模态有」的文案做人工确认后，再写入最终 UI Spec。
